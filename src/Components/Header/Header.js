import React from 'react';
import styles from './Header.css';

const Header = () => {

    return (
        <div>
            <h1>
                Memory Game!
            </h1>
            <h2>
                Click on an image to earn points, but don't click on any image than once!
            </h2>
        </div>
    )
}

export default Header;